apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "dependencytrack-operator.fullname" . }}-apikey-admin-role
  labels:
  {{- include "dependencytrack-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - apikeys
  verbs:
  - '*'
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - apikeys/status
  verbs:
  - get
