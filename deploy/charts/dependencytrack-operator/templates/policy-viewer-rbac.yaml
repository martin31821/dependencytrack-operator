apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "dependencytrack-operator.fullname" . }}-policy-viewer-role
  labels:
  {{- include "dependencytrack-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - policies
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - policies/status
  verbs:
  - get
