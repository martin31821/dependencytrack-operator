apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "dependencytrack-operator.fullname" . }}-notificationrule-admin-role
  labels:
  {{- include "dependencytrack-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - notificationrules
  verbs:
  - '*'
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - notificationrules/status
  verbs:
  - get
