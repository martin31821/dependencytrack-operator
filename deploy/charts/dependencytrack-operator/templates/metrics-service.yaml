apiVersion: v1
kind: Service
metadata:
  name: {{ include "dependencytrack-operator.fullname" . }}-controller-manager-metrics-service
  labels:
    control-plane: controller-manager
  {{- include "dependencytrack-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    app.kubernetes.io/name: deptrack-operator
    control-plane: controller-manager
    {{- include "dependencytrack-operator.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.metricsService.ports | toYaml | nindent 2 }}
