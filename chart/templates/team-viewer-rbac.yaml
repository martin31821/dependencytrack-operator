apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "deptrack-operator.fullname" . }}-team-viewer-role
  labels:
  {{- include "deptrack-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - teams
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - teams/status
  verbs:
  - get
