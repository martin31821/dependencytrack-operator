apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "deptrack-operator.fullname" . }}-team-admin-role
  labels:
  {{- include "deptrack-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - teams
  verbs:
  - '*'
- apiGroups:
  - dependencytrack.mko.dev
  resources:
  - teams/status
  verbs:
  - get
